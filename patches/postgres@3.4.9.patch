diff --git a/src/connection.js b/src/connection.js
index 1b1cccde43b4570d5d071d6ffaab2669b3c2065a..89dce370d7f38d3cf47632c3c1fc1e52dd0b30ce 100644
--- a/src/connection.js
+++ b/src/connection.js
@@ -383,6 +383,11 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       return
 
     errored(err)
+    // Every route into error() is a dead or closing socket, so no ReadyForQuery is coming to
+    // clear the rejected query, and end() would wait on it forever. It has to be cleared here
+    // rather than in errored(), which is also reached with the connection alive — execute()'s
+    // catch recovers by writing a Sync, and that Sync's ReadyForQuery still has to find it.
+    query = null
     while (sent.length)
       queryError(sent.shift(), err)
   }
@@ -437,6 +442,13 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
     incoming = Buffer.alloc(0)
     remaining = 0
     incomings = null
+    // A postgres error is held until the ReadyForQuery that ends the query it belongs to, and a
+    // backend that dies mid-query sends its FATAL and then goes away without sending one. Left
+    // set, it is read by the first ReadyForQuery to arrive on the NEXT socket -- the handshake's
+    // -- and rejects that connection's startup query, which is the pool's oldest queued work, with
+    // the error of a backend it never reached. Reset beside the other per-socket parse state
+    // rather than below, so the connect-phase close that returns early is covered too.
+    errorResponse = null
     clearImmediate(nextWriteTimer)
     socket.removeListener('data', data)
     socket.removeListener('connect', connected)
@@ -451,6 +463,13 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
       return reconnect()
 
     !hadError && (query || sent.length) && error(Errors.connection('CONNECTION_CLOSED', options, socket))
+    // A socket that has gone away brings no ReadyForQuery, and end() hands back the ending it
+    // already made rather than retaking its fast path, so terminate(), which is what would
+    // otherwise invoke ended(), is out of reach from here and that end() would never settle.
+    // Settled before onclose(), the conservative order, in case onclose() hands this connection
+    // a new query. A connection still in its connect phase takes the return above and is not
+    // covered — patches/README.md carries that rule, and Overflow issue 164 tracks it.
+    ended && (ended(), ending = ended = null)
     closedTime = performance.now()
     hadError && options.shared.retries++
     delay = (typeof backoff === 'function' ? backoff(options.shared.retries) : backoff) * 1000
@@ -564,9 +583,16 @@ function Connection(options, queues = {}, { onopen = noop, onend = noop, onclose
         return fetchArrayTypes()
       }
 
-      initial && !initial.reserve && execute(initial)
+      // A reserve is not a query to execute: it is settled by handing this connection to the
+      // pool, which is what onopen does. Skipping both -- not executing it and not opening --
+      // leaves the reserve waiting on an onopen that never comes and this connection stuck in
+      // the pool's connecting queue. A pool that is already ending gets the connection closed,
+      // exactly as the bottom of this function does for every other opening connection.
+      const opening = initial.reserve
+      !opening && execute(initial)
       options.shared.retries = retries = 0
       initial = null
+      opening && (ending ? terminate() : onopen(connection))
       return
     }
 
diff --git a/src/index.js b/src/index.js
index c7fba3dace20173f4cb13f3f138bb6d213b43683..e67184588053b7cd31570e4c8e9a05f599ada000 100644
--- a/src/index.js
+++ b/src/index.js
@@ -205,19 +205,37 @@ function Postgres(a, b) {
     const c = open.length
       ? open.shift()
       : await new Promise((resolve, reject) => {
-        const query = { reserve: resolve, reject }
+        // A reserve rejected while still queued has to leave `queries` too: onopen settles a
+        // reserve by shifting it out and returning, so a rejected one left behind swallows the
+        // connection that reaches it -- neither served nor moved to `open`, the slot is lost.
+        const query = { reserve: resolve, reject: (e) => (queries.remove(query), reject(e)) }
         queries.push(query)
         closed.length && connect(closed.shift(), query)
       })
 
     move(c, reserved)
-    c.reserved = () => queue.length
+    const reservation = c.reserved = () => queue.length
       ? c.execute(queue.shift())
       : move(c, reserved)
     c.reserved.release = true
 
     const sql = Sql(handler)
     sql.release = () => {
+      // The pool takes a reservation back on its own in onclose, by nulling `c.reserved`, and it
+      // does that while the connection has no socket. Handing it back a second time from here runs
+      // onopen on it, which dispatches queued work whose write lands in a setImmediate that reads
+      // `socket.write` off null and throws where no caller can catch it -- the process exits.
+      // Guarded here rather than at that write because a guard there stops only the throw: the
+      // buffered bytes and the scheduled-write handle both survive the close, and write() arms a
+      // flush only while that handle is null, so the reconnect's StartupMessage is appended behind
+      // them and never sent -- the pool then refuses everything queued on it with CONNECT_TIMEOUT.
+      // Compared by identity rather than truthiness, because `c.reserved` is also how a *later*
+      // reserve() or a begin() marks this same connection as its own: a spent release seeing one
+      // of those would cancel a reservation it does not hold and hand one backend to two callers
+      // that each believe it is theirs alone. Nulling it is what makes a second release inert too.
+      if (c.reserved !== reservation)
+        return
+
       c.reserved = null
       onopen(c)
     }
@@ -423,7 +441,11 @@ function Postgres(a, b) {
     c.reserved = null
     c.onclose && (c.onclose(e), c.onclose = null)
     options.onclose && options.onclose(c.id)
-    queries.length && connect(c, queries.shift())
+    // A reserve pseudo-query is only ever settled by onopen, which reads it back out of
+    // `queries`; shifting one out here to hand the reconnect as its `initial` drops it, and its
+    // caller waits forever. reserve() already passes a still-queued reserve to connect().
+    const head = queries.peek()
+    queries.length && connect(c, head.reserve ? head : queries.shift())
   }
 }
 
diff --git a/src/queue.js b/src/queue.js
index c4ef97164f057db99dce1b9af3401dbb1af37e31..8fba891b879e8f5c7fdb03ea0590753663c824c3 100644
--- a/src/queue.js
+++ b/src/queue.js
@@ -15,6 +15,7 @@ function Queue(initial = []) {
         : (xs.splice(index, 1), x)
     },
     push: (x) => (xs.push(x), x),
+    peek: () => xs[index],
     shift: () => {
       const out = xs[index++]
 
